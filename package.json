{
  "name": "void-desktop",
  "version": "0.1.0",
  "private": true,
  "main": "electron-main.js",
  "scripts": {
    "dev": "concurrently \"node server/index.js\" \"vite\" \"wait-on http://localhost:5173 && electron .\"",
    "start-server": "node server/index.js",
    "build:ui": "cd renderer && vite build",
    "electron:prod": "electron .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "axios": "^1.5.0",
    "better-sqlite3": "^8.0.1",
    "express": "^4.18.2",
    "openai": "^4.10.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "electron": "^26.0.0",
    "wait-on": "^7.0.1",
    "vite": "^5.1.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
